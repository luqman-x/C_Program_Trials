#include <stdio.h>
#include <stdlib.h>
char Search(int size, char name, char target)
{
    left = 0;
    right = size -1;
    while(lower>= upper_bound)
    {
        if ()
    }
//int res = strcmp(names[i], target);
}

int main()
{

 char name[10][20] = {"Alice Brown", "David Smith", "John Doe", "Michael Johnson", "Zara White"};

 char target = "John Doe";
 int size = sizeof(char name)/sizeof(char name[0][]);
}
